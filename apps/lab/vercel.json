{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter lab build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/lab/build",
  "framework": "docusaurus"
}
