{
  "buildCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install && pnpm --filter portfolio build",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/portfolio/.next"
}
