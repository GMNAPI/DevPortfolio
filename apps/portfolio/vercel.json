{
  "buildCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && cd ../.. && pnpm install && pnpm --filter portfolio build",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && cd ../.. && pnpm install",
  "framework": "nextjs"
}
